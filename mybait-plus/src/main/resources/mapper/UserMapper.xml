<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baomidou.mybatisplus.samples.deluxe.mapper.UserMapper">

    <select id="selectUserPage" resultType="com.baomidou.mybatisplus.samples.deluxe.entity.User">
        select *
        from user
        <trim prefix="where" prefixOverrides="AND">
            <if test="selectInt != null">
                age = #{selectInt}
            </if>
            <if test="selectStr != null">
                AND name = #{selectStr}
            </if>
            AND deleted = 0
        </trim>
    </select>

    <!-- 通用查询映射结果 -->
    <resultMap id="userResultMap" type="com.baomidou.mybatisplus.samples.deluxe.entity.User">
        <id column="id" property="id" />
        <result column="tags" property="tags" typeHandler="com.baomidou.mybatisplus.samples.deluxe.handler.ListJsonHandler"/>
        <result column="email" property="email" typeHandler="com.baomidou.mybatisplus.samples.deluxe.config.TestTypeHandler"/>
        <result column="attributes" property="attributes" typeHandler="com.baomidou.mybatisplus.samples.deluxe.handler.MapJsonHandler"/>
    </resultMap>

</mapper>